services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: manage_fees
      POSTGRES_PASSWORD: manage_fees
      POSTGRES_DB: manage_fees
    ports:
      - "5433:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
